{% extends 'GitPhpBundle:Main:base.html.twig' %}
{% block body %}

    <div class="container">
        <div class="row">
            {% if dataArray.status == "OK" %}
                <div class="col-lg-12">
                    <h2>Successfully commited files to repo!</h2>
                </div>
                <div class="col-lg-12">
                    <pre>{{ dataArray.commitOutput }}</pre>
                </div>
                <div class="col-lg-12 text-center">
                    <h1><i class="fa fa-check-circle-o fa-3x text-success" aria-hidden="true"></i></h1>
                </div>
            {% else %}
                <div class="col-lg-12">
                    <h2>Error committing files!</h2>
                </div>
                <div class="col-lg-12">
                    <pre>{{ dataArray.error }}</pre>
                </div>
                <div class="col-lg-12 text-center">
                    <h1><i class="fa fa-times-circle fa-3x text-danger" aria-hidden="true"></i></h1>
                </div>
            {% endif %}
        </div>
    </div>

{% endblock %}